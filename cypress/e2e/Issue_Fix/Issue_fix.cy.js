describe("RadioButton", () => {
    beforeEach("Visit the URL", () => {
        // Disable the SSL verification for an insecure connection
        cy.visit("/", {
            failOnStatusCode: false,
        });
    });

    it("Check box", () => {
        // Your test logic goes here
    });
});
